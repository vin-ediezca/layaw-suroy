<% content_for :title, "Add a Category" %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <ul class="nav nav-tabs">
      <li role="presentation" class="active"><a href="#">Add a Category</a></li>
    </ul>
  </div>
</div>

<div class="row">
  <div class="col-md-6 col-md-offset-3">  
    <div class="panel panel-default">
      <div class="panel-body">

        <%= render 'shared/errors', obj: @category %>
        <%= render 'shared/flash' if flash_list %>

        <%= form_with model: @category, url: {action: "create"}, html: {class: ""}, local: true do |f| %>
          <div class="form-group">
            <div class="input-group">
              <span class="input-group-addon" id="basic-addon1"><strong>Category Name</strong></span>
              <%= f.text_field :name, class: "form-control" %>
            </div>
          </div>
          <div class="">
            <%= f.button "Add", class: "btn btn-primary", data: {disable_with: "<i class='fas fa-spinner fa-spin'></i> Adding..."} %>
            <%= link_to "Cancel", root_path, data: {confirm: "Are you sure you want to cancel?"}, class: "btn btn-default" %>
          </div>
        <% end %>

      </div>
    </div>
  </div>
</div>